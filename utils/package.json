{
  "name": "corewar-utils",
  "version": "1.0.0",
  "description": "Corewar utilities",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "chalk": "^2.4.2",
    "json-diff": "^0.5.4",
    "tmp-promise": "^2.0.1"
  }
}
